{
  "name": "apmldevsite",
  "version": "1.0.0",
  "description": "Official home of APML v0.9.1 - Triadic architecture for human-AI collaborative problem solving",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/enhanced-apml-validator.js",
    "validate:all": "node scripts/validate-all-apml.js",
    "compile": "node scripts/ultimate-apml-compiler.js",
    "preview:app": "npm run validate:all && npm run compile && npm run build && npm run preview",
    "deploy": "npm run validate:all && npm run compile && npm run build"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "unocss": "^0.58.0",
    "@unocss/preset-typography": "^0.58.0",
    "@unocss/preset-web-fonts": "^0.58.0"
  }
}